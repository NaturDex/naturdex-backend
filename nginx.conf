events {}

http {
    server {
        listen 80;

        location / {
            proxy_pass http://api:5000;
        }

        location /metrics {
            auth_basic "Metrics";
            auth_basic_user_file /etc/nginx/htpasswd;
            proxy_pass http://api:5000/metrics;
        }
    }
}